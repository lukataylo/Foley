# secrets
SECRETS.md
.env
.env.*
!.env.example

# node
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# python
__pycache__/
*.py[cod]
.venv/
.uv-cache/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# remotion
remotion-out/

# generated walkthrough artifacts
# Step-level captures are derived data; ignore them.
walkthroughs/*/steps/*.webm
walkthroughs/*/steps/.narration-cache/
# Render runtime state — recreated each run, not part of the deliverable.
walkthroughs/*/.render-status.json
walkthroughs/*/.render.log
# Take artifacts ARE the demo deliverable — track them so the cutroom can be
# deployed as a read-only public site. (Use git LFS at scale.)

# scratch
.DS_Store
*.log
.tmp/
scratch/
